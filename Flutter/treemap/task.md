## Common Issues

| Issue | Pages Affected | Fix |
|---------|---------------|-----|
| All code blocks are incomplete. They are missing the required import statements (e.g., `package:flutter/material.dart` and the SignaturePad package import) and do not include the complete widget class structure. Most snippets contain only the `build()` method or partial widget implementation. | All affected pages containing code snippets | Update all code samples to include the required import statements and provide complete, runnable widget examples with the appropriate `StatefulWidget` or `StatelessWidget` wrapper. |
| `platform: Flutter` is used in the front matter. | All applicable pages | Change `platform: Flutter` to `platform: flutter`. |
| `highlight Dart` is used for code blocks. | All pages containing Dart code samples | Change `highlight Dart` to `highlight dart`. |
| Code samples are not directly runnable and lack sufficient context for users. | All pages containing code snippets | Ensure every code sample is complete, self-contained, and can be copied and executed without requiring additional code from other sections. |